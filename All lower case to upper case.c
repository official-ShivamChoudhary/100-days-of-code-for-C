#include<stdio.h>
#include<conio.h>
void main()
{
	char str;
	printf("Enter a string:-");
	scanf("%s",str);
    to str upper(char[]);
	printf("Changed to:-%s",str);
	}
	void to str upper(char[])
	{
		int i=0;
		while(s[i]>'a) && (s[i]>='z'))
		s[i]=s[i]-32;
		i++;
		}
		}
		
		
	
